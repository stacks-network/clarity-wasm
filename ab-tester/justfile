set dotenv-load

run *args:
    RUST_BACKTRACE=full RUST_LOG="debug" cargo run -- {{args}}

fmt:
    cargo fmt -- --config group_imports=StdExternalCrate,imports_granularity=Module

check:
    cargo clippy --manifest-path=./Cargo.toml --no-deps --all-features --all-targets -- -D warnings