(define-public (let-ints)
  (let ((x 1) (y 2))
    (ok (+ x y))
  )
)

(define-public (nested-lets)
  (let ((x 21))
    (let ((y 22))
      (ok (+ x y))
    )
  )
)

(define-public (let-use-binding)
  (let ((a 5)
        (c (+ a 1))
        (d (+ c 1))
        (b (+ a c d)))
    (ok (+ a b))
  )
)
